<?xml version="1.0"?>
<launch>

	<arg name="config" default="car" doc="rviz config file name in gigatron/rviz" />

	<!-- launch rviz -->
	<node name="rviz" pkg="rviz" type="rviz" args="-d $(find gigatron)/rviz/$(arg config).rviz" output="screen" />

</launch>
